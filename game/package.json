{
  "name": "me-vertical-slice-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.js",
    "test": "node --test tests/*.test.js",
    "verify": "node --test tests/*.test.js && node scripts/check-requirements.js"
  }
}
